.well{style: "float:right"}
  %a{href: new_pet_path}
    Submit a Link
//%div{ style:"margin-bottom: 30px"}
%ul
  - @pets.each do |pet|
    %li{style:"margin-bottom: 20px"}
      %p
        %a{href: pet.url} 
          =pet.title
      %p
        %a{href: pet_path(pet)}
          = "#{pet.comments.count} 评论" 
