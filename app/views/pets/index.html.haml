.well{style: "float:right"}
  %a{href: new_pet_path}
    Submit a Link
%table.table-striped
  - @pets.each do |pet|
    %tr{style:"margin-bottom: 20px"}
      %td
        = render pet.vote 

      %td
        %p
          %a{href: fix_url(pet.url)} 
            = pet.title
        %p
          by 
          =pet.user.username
        %p
          %a{href: pet_path(pet)}
            = "#{pet.comments.count} 评论" 
